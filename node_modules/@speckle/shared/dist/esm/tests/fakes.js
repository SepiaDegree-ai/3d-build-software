import { merge } from 'lodash';
export const fakeGetFactory = (defaults) => (overrides) => () => {
    if (overrides) {
        return Promise.resolve(merge(defaults, overrides));
    }
    return Promise.resolve(defaults);
};
export const getProjectFake = fakeGetFactory({
    isPublic: false,
    isDiscoverable: false,
    workspaceId: null
});
//# sourceMappingURL=fakes.js.map